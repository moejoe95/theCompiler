before_script:
  - apt-get update -qq && apt-get install -y -qq python3 python3-pip ninja-build
  - pip3 install meson

stages:
  - build

meson_build:
  stage: build
  script:
    - meson builddir
    - ninja -C builddir